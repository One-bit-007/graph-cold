# Core
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
pyyaml>=6.0
tqdm>=4.65

# Deep learning
# Default PyPI torch wheels are CPU-capable. For CUDA, install the wheel that
# matches the local driver, for example:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.1
# Heterogeneous / temporal graph learning
# Install torch-geometric after torch so its optional CUDA extensions match the
# selected PyTorch/CUDA build; see https://pytorch-geometric.readthedocs.io/.
torch-geometric>=2.4
networkx>=3.1

# Baselines / label-noise utilities
cleanlab>=2.6          # confident-learning reference for label error detection
xgboost>=2.0           # OpTC downstream classifier (Flash/Argus plug-in)

# Experiment tracking / plotting
matplotlib>=3.7
seaborn>=0.12

# Testing
pytest>=8.0
