# Prompt archive

Handoff prompts, one file per role per day. Naming: `prompt_<role>_dX.md`.

- GPT-pro authors `prompt_codex_dX.md` from D2 onward (see `prompt_gptpro_d1.md`).
- Copilot (coordinator) reviews each against the key checkpoints (CK-1..CK-7)
  before the day's work is tagged.
