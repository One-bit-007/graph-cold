# Codex Prompt — P3 SUBMISSION-FIX (pre-submission consistency: items 1–9)

> This round fixes the 9 textual / structural issues identified by the senior
> C&S pre-submission audit. It does NOT touch model code, Graph-CDM, encoder,
> noise model, metrics implementations, or any results/*.csv/json.
> Branch `fix/p3-submission-consistency`, push, tag `rev-p3`.
> INTEGRITY: no results may be changed; only LaTeX prose, file organisation and
> the reproducibility README are modified.

## Files you must read first
paper/graph_cold_cas_final.tex       ← the ONE submission file; fix all edits here
reports/d9/d9_submission_lock_audit.json
reports/p2f_tighten.json             ← authoritative scope & seeds
tables/table_p2f_summary.csv         ← authoritative P2f results
tables/table_p2f_powered_tests.csv
tables/table_p2d_per_dataset_vs_cold.csv
reports/p2b_baseline_fidelity.md     ← MCRe/MORSE caveat text
reproducibility/README_realdata.md
reproducibility/run_d5_realdata.ps1

============================================================
# FIX 1 — Baseline overclaim: remove non-existent baselines from §5.2
============================================================
Problem: lines 293–297 of paper/graph_cold_cas_final.tex claim MCRe, MORSE,
Co-teaching, FINE, and Decoupling as "implemented real-data baselines", but the
headline results (Table 2, Table 3) come exclusively from
tables/table_p2f_summary.csv which contains only CoLD / ablation_hard /
Graph-CoLD-soft / Graph-CoLD-semisup.

Action:
- Replace the §5.2 baseline list so it exactly matches the P2f scope:
  "We compare Graph-CoLD (soft and semi-supervised variants) with the aligned
  CoLD baseline and the \texttt{ablation\_hard} deletion variant across the
  Tables 2–3 result matrix."
- After that sentence, add one acknowledgement paragraph noting that additional
  purification methods (MCRe, MORSE, FINE, Decoupling, Co-Teaching-lite) were
  evaluated under an earlier protocol; they are not re-run under the P2f
  clean-deduplication protocol and are therefore not included in Tables 2–3 to
  avoid mixing incompatible result regimes. Cite reports/p2b_baseline_fidelity.md
  for the MCRe/MORSE caveat.
- Keep §2 Related Work citations to these methods unchanged (they are background
  citations, not baseline claims).

Acceptance: §5.2 no longer names MCRe/MORSE/FINE/Decoupling/Co-teaching as
formal compared baselines for Tables 2–3. The change is clearly reasoned in the
text, not silently dropped.

============================================================
# FIX 2 — Co-Teaching name: use "Co-Teaching-lite" everywhere in §5
============================================================
Problem: §5.2 writes "Co-teaching~\cite{han2018coteaching}" implying the full
implementation; d9_submission_lock_audit.json confirms co_teaching_lite_not_full=true.

Action:
- In §5.2 (after FIX 1), if Co-Teaching-lite is still mentioned as context
  (e.g., in the earlier-protocol acknowledgement), write it as
  "Co-Teaching-lite (a lightweight approximation)" and add a parenthetical note
  that it is not a full re-implementation of Han et al.
- Search the full paper for any occurrence of "Co-teaching" or "Co-Teaching" and
  ensure none incorrectly implies full implementation.

Acceptance: "Co-Teaching-lite" or "lightweight approximation" appears wherever the
method is named in the experimental context; §2 Related Work citation is unchanged.

============================================================
# FIX 3 — Scope unification: align paper with P2f (3 datasets, 4 methods)
============================================================
Problem: d9 audit locked 2 datasets (CICIDS + CESNET); p2f_tighten.json and
table_p2f_summary.csv show 3 datasets (CICIDS + CESNET + UNSW-NB15); the paper
uses 3 datasets but §5.2 lists non-P2f baselines. Establish one authoritative
scope.

Action (paper only, no code change):
- §5.1 already correctly lists 3 datasets with audit-window protocol. Ensure §1
  Introduction and §10 Conclusion also reference "three real datasets". Check for
  any stray "two-dataset" phrasing and update.
- Add one sentence at the end of §5.1 explicitly tying the result tables to P2f:
  "All results in Tables 2–3 and Figures 2–3 are generated by the P2f clean
  de-oracled runner (reports/p2f_tighten.md) on these three datasets; earlier
  intermediate result files (D5/D9) used a different protocol and must not be
  cited as final results."
- Update §8 Threats to Validity to note that d9_submission_lock.json's
  "two-dataset" lock was an intermediate checkpoint; the authoritative final scope
  is P2f.

Acceptance: paper consistently references 3 datasets; no "two-dataset" phrasing;
Tables 2–3 trace to P2f.

============================================================
# FIX 4 — Archive old manuscripts: prevent accidental submission of wrong version
============================================================
Problem: paper/elsevier/ and paper/sections/ contain stale oracle-era manuscripts
(graph_cold_cas_realdata.tex, graph_cold_cas_realdata_reinforced.tex, d11
variants) that contradict the final submission file.

Action:
- Create directory paper/archive_do_not_submit/
- Move (git mv) all files under paper/elsevier/ and paper/sections/ into
  paper/archive_do_not_submit/ (preserve them, do not delete).
- Add a paper/archive_do_not_submit/README.txt with content:
  "These are intermediate drafts (D8/D9/D11 era). They use an earlier result
  protocol and may contain inconsistencies with the final manuscript.
  DO NOT submit any file from this folder. The submission file is
  paper/graph_cold_cas_final.tex"
- Keep paper/graph_cold_cas_final.tex, .pdf, .aux, .log, .out, .spl at the
  paper/ root. The elsarticle.cls copy stays at paper/ root too.

Acceptance: no tex file outside paper/graph_cold_cas_final.tex is at the paper/
root or an unarchived location. git status shows only moves, no deletions.

============================================================
# FIX 5 — rare-recovery: move to appendix or footnote, remove from main flow
============================================================
Problem: §6.2 discusses rare-evidence recovery in the main text even after the
audit showed soft recovery ≈ ceiling (0.97–1.0). Auditor flagged it as still
resembling a circular metric even after redefinition.

Action:
- Keep the current honest caveat sentence in §6.2: "An earlier rare-evidence
  recovery rate ... was tautological. We redefine recovery to require correct
  downstream classification ..." — this is necessary for integrity.
- AFTER that sentence, compress the diagnostic result to one sentence:
  "Because soft retention still scores near-ceiling on this diagnostic
  (secondary tables in supplementary), we rely solely on test-set tail-F1 for all
  headline comparisons."
- Move any numeric rare-recovery numbers (e.g., "0.97–1.0") and the reference to
  tables/table_p2f_summary.csv recovery columns to a supplementary note or footnote.
- Do NOT remove the concept; just ensure the main text carries no numeric
  rare-recovery claim that could be read as a headline contribution.

Acceptance: §6.2 main text contains no numeric rare-recovery headline; the
concept is acknowledged, caveated, and relegated to supplementary/footnote.

============================================================
# FIX 6 — CoLD encoder fairness: add one explicit sentence to §4 or §5
============================================================
Problem: The paper does not state that CoLD's representation learning uses the
same encoder and training budget as Graph-CoLD, which is required for the ablation
to be convincing.

Action:
- In §4.2 (Self-supervised representation) or §5.2, add one sentence after the
  ablation_hard description:
  "Both the aligned CoLD baseline and the \texttt{ablation\_hard} variant use the
  same shared encoder architecture, training budget, and view-masking configuration
  as Graph-CoLD; they differ only in the presence or absence of graph views and the
  weighting rule applied after Graph-CDM scoring."

Acceptance: CoLD encoder-fairness claim is explicit in the paper.

============================================================
# FIX 7 — Protocol transparency table: add condensed protocol paragraph to §5.1
============================================================
Problem: seeds, n-per-test, paired keys, and CI method are scattered across §5.1,
§5.2, and the Table 3 caption. A careful reviewer can verify them but must hunt.

Action:
- At the end of §5.1, add a short "Reproducibility and statistical protocol"
  paragraph (3–5 sentences) that explicitly states:
  (a) data versions and root (local; not committed to repository);
  (b) train 10,000 / test 5,000 deterministic audit windows;
  (c) asymmetric tail-noise rates {40, 60, 80}%, seeds 0–9;
  (d) paired at scenario level (one observation = mean over seeds per
      dataset/noise-type/noise-rate), n=30 per dataset;
  (e) Holm–Bonferroni correction; bootstrap 95% CI;
  (f) full reproduction via `reports/p2f_tighten.md` commands.

Acceptance: all statistical protocol details are in one paragraph; Table 3 caption
can refer to it.

============================================================
# FIX 8 — README: mark P2f as the authoritative result source
============================================================
Problem: reproducibility/README_realdata.md does not explicitly distinguish the
P2f clean-deduplication protocol from the earlier D5/D9 protocols. A user could
accidentally regenerate stale results.

Action: In reproducibility/README_realdata.md, add a clearly marked section
titled "## Authoritative result source" near the top (after the Overview),
containing:
- "The submission results in Tables 2–3 of the paper are generated by the P2f
  clean-deduplication protocol (reports/p2f_tighten.md). Do NOT use the D5 or D9
  scripts (reproducibility/run_d5_realdata.ps1, run_d5_baseline_expansion.ps1)
  to regenerate submission results; those scripts target earlier intermediate
  protocols that predate the leakage audit and may produce different numbers."
- "To reproduce the submission tables and figures, run the commands listed in
  reports/p2f_tighten.md §Reproduction Commands."
- "The P2d, P2e, and P2f reports explain the audit history and why earlier numbers
  were discarded."

Acceptance: README has an "Authoritative result source" section; run_d5 scripts
are clearly marked as non-submission-path.

============================================================
# FIX 9 — CoLD reference [1]: mark as "to appear" and add DOI placeholder
============================================================
Problem: \bibitem{yang2026cold} currently lists only the venue name without volume,
pages, or DOI. NDSS 2026 proceedings are not yet formally published.

Action:
- In the thebibliography, update [1] to:
  S. Yang, X. Zheng, J. Li, J. Xu, E. C. H. Ngai,
  ``CoLD: Collaborative label denoising framework for network intrusion detection,''
  in \emph{Proc. Network and Distributed System Security (NDSS) Symposium}, 2026,
  to appear. Available: \url{https://dx.doi.org/10.14722/ndss.2026.231950}
  (The DOI 10.14722/ndss.2026.231950 is the official NDSS 2026 DOI from the PDF
  and is verifiable.)
- Do NOT fabricate page numbers or volume; "to appear" is correct for a 2026 paper.

Acceptance: [1] includes "to appear", the verified DOI, and no fabricated fields.

============================================================
# COMPILE AND VERIFY
============================================================
After all edits:
1. Run `pdflatex -interaction=nonstopmode paper/graph_cold_cas_final.tex` twice.
2. Confirm "Output written on graph_cold_cas_final.pdf" with zero "undefined
   reference" or "undefined citation" lines.
3. Run `grep -n "MCRe\|MORSE\|FINE\|Decoupling" paper/graph_cold_cas_final.tex`
   and confirm no occurrence is inside \subsection{Baselines} or a results-claim
   sentence (Related Work occurrences are expected and fine).
4. Run `grep -in "co-teaching[^-]" paper/graph_cold_cas_final.tex` and confirm
   all occurrences in §5+ are followed by "-lite" or "lightweight".
5. Confirm paper/elsevier/ and paper/sections/ no longer exist at their original
   paths (moved to archive_do_not_submit/).

============================================================
# OUTPUT REPORT — paper/p3_fix_report.txt (plain text, commit it)
============================================================
List:
1. Each fix: file edited, line range changed, brief description.
2. Compile result (page count, zero undefined).
3. grep evidence for FIX 1 and FIX 2 checks.
4. Confirmation that FIX 4 moved files (git mv output).
5. Any item you could NOT complete and why.

## Git
- branch: fix/p3-submission-consistency
- commits: one per fix group (1–3, 4, 5–9), e.g.
  "fix(p3-1-3): converge baselines, scope, co-teaching-lite in §5"
  "fix(p3-4): archive stale elsevier/sections drafts"
  "fix(p3-5-9): rare-recovery, encoder fairness, protocol table, README, bib"
- merge main, tag rev-p3, push origin main/dev/tags.

## Constraints
- Do NOT change model code, Graph-CDM, encoder, noise model, metrics, or
  results/*.csv/json.
- Do NOT generate or fabricate any numbers.
- Do NOT set submission_ready=true (leave d9 audit json unchanged).
- Only paper/graph_cold_cas_final.tex, reproducibility/README_realdata.md,
  and paper/archive_do_not_submit/* are modified or created.
